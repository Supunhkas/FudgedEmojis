import { createTheme } from '@mui/material/styles';

const CustomTheme = createTheme({
  palette: {
    primary: {
      main: '#1DBF73', // Replace with your desired color
    },
  },
});

export default CustomTheme;
